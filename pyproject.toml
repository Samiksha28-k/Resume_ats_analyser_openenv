[project]
name = "resume-ats-analyser"
version = "0.1.0"
description = "Resume ATS analyser"
requires-python = ">=3.10,<3.12"

dependencies = [
    "fastapi",
    "uvicorn",
    "transformers",
    "torch==2.2.2",
    "python-multipart",
    "pydantic",
    "huggingface-hub"
]

[tool.uv]
dev-dependencies = []
