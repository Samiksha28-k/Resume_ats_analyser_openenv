[project]
name = "resume-ats-analyser"
version = "0.1.0"
description = "Resume ATS Analyser using FastAPI"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "transformers",
    "python-multipart",
    "huggingface_hub",
    "openenv-core>=0.2.0",
    "torch==2.2.2+cpu"
]

[project.scripts]
server = "server.app:main"

[tool.uv]
dev-dependencies = []