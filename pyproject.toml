[project]
name = "resume-ats-analyser"
version = "0.1.0"
description = "Resume ATS Analyser using FastAPI"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "transformers",
    "torch",
    "python-multipart",
    "huggingface_hub",
    "openenv-core>=0.2.0"
]

[project.scripts]
<<<<<<< HEAD
server = "server.app:main"

[tool.uv]
dev-dependencies = []
=======
server = "server.app:app"

[tool.uv]
dev-dependencies = []
>>>>>>> 67344d8 (added pyproject and uv lock)
